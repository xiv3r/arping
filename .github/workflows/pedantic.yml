name: Pedantic check

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']
jobs:
  build:
    runs-on: ubuntu-latest
    stops:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Pedantic check
        run: ./extra/pedantic.sh
